specVersion: "v1beta"
serviceId: "palermo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "palermo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
